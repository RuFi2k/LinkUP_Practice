import 'package:flutter/material.dart';

class AvgTitle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Avg Expenses',
        style: TextStyle(
          fontSize: 16,
          color: Color(0xFFBECFE7),
        ),
      ),
    );
  }
}
